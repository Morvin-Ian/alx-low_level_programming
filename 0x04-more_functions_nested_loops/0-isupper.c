#include "main.h"
#include <stdlib.h>

/**
 * _isupper - adds two integers
 * @c: integer 1
 *
 * Description: Checks if a number is upper
 * Return: 1 or 0
 */

int _isupper(char c)
{
	
	if (c >= 65 && c <= 90)
		return (1);
	else
		return (0)
}
